<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#4393af"
    >

    <Button
        android:id="@+id/Meat"
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:layout_alignParentLeft="true"
        android:layout_alignParentTop="true"
        android:layout_gravity="center"
        android:layout_marginLeft="15dp"
        android:layout_marginTop="149dp"
        android:drawableTop="@drawable/meat_btn"
        android:fontFamily="@font/ya"
        android:paddingTop="20dp"
        android:text="Meat"
        android:textSize="20dp" />

    <Button
        android:id="@+id/not_Meat"
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:layout_alignParentRight="true"
        android:layout_alignTop="@+id/Meat"
        android:layout_gravity="center_vertical"
        android:layout_marginRight="11dp"
        android:drawableTop="@drawable/not_meat_btn"
        android:fontFamily="@font/ya"
        android:paddingTop="20dp"
        android:text="Not  meat"
        android:textSize="20dp"/>

    <Button
        android:id="@+id/meat_Back"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="55dp"
        android:text="back"
        tools:ignore="RtlCompat" />

</RelativeLayout>